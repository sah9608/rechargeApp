<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.recharge.notice.dao.NoticeDAO">

     <!--공지사항추가 -->
    <insert id="insertNotice" parameterType="com.recharge.notice.vo.NoticeVO">
        <selectKey keyProperty="noticeId" resultType="java.lang.Long" order="BEFORE">
            SELECT SEQ_NOTICE_ID.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO TB_NOTICE(
          NOTICE_ID,
          NOTICE_TITLE,
          NOTICE_CONTENT,
          USER_ID,
          NOTICE_VIEW_COUNT,
          CREATE_DATE,
          CREATE_ID,
          UPDATED_DATE,
          UPDATED_ID
        ) VALUES(
          #{noticeId},
          #{noticeTitle},
          #{noticeContent},
          #{userId},
          #{noticeViewCount},
          SYSDATE,
          #{createId},
          SYSDATE,
          #{updatedId}
        )
    </insert>

    <!-- 공지사항 목록조회 -->

    <select id="getNoticeList"  resultType="NoticeVO">
        SELECT
         NOTICE_ID AS noticeId,
         NOTICE_TITLE AS noticeTitle,
         NOTICE_CONTENT AS noticeContent,
         USER_ID AS userId,
         NOTICE_VIEW_COUNT AS noticeViewCount,
         CREATE_DATE AS createDate,
         CREATE_ID AS createId,
         UPDATED_DATE AS updatedDate,
         UPDATED_ID AS updatedId
        FROM TB_NOTICE
        ORDER BY CREATE_DATE DESC
    </select>

    <!-- 공지사항 상세조회-->

    <select id="getNoticeDetail" parameterType="Long" resultType="NoticeVO">
        SELECT
          NOTICE_ID AS noticeId,
          NOTICE_TITLE AS noticeTitle,
          NOTICE_CONTENT AS noticeContent,
          NOTICE_VIEW_COUNT AS noticeViewCount,
          CREATE_DATE AS createDate,
          CREATE_ID AS createId,
          UPDATED_DATE AS updatedDate,
          UPDATED_ID AS updatedId
        FROM TB_NOTICE
        WHERE NOTICE_ID = #{noticeId}
    </select>

    <!-- 공지사항 글 수정 -->
    <update id="editNoticeDetail" parameterType="NoticeVO">
        UPDATE TB_NOTICE
        SET
          NOTICE_TITLE = #{noticeTitle},
          NOTICE_CONTENT = #{noticeContent},
          UPDATED_DATE = SYSDATE,
          UPDATED_ID = #{updatedId}
        WHERE NOTICE_ID = #{noticeId}

    </update>

   <!--공지사항 글 삭제 -->

    <delete id="deleteNoticeDetail" parameterType="Long">
        DELETE FROM TB_NOTICE
        WHERE NOTICE_ID =#{noticeId}
    </delete>

    <!-- 조회수 증가 후 현재 조회수 반환 -->
    <update id="incrementViewCount" parameterType="long" >
        UPDATE TB_NOTICE
        SET NOTICE_VIEW_COUNT = NOTICE_VIEW_COUNT + 1
        WHERE NOTICE_ID = #{noticeId}
    </update>

    <select id="getNoticeDetailViewCount" parameterType="long" resultType="long">
        SELECT NOTICE_VIEW_COUNT
        FROM TB_NOTICE
        WHERE NOTICE_ID = #{noticeId}
    </select>

</mapper>